{
  "framework": "nextjs",
  "buildCommand": "pnpm install && pnpm run build:vercel",
  "installCommand": "pnpm install",
  "regions": ["hkg1"],
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}
